# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Data artifacts that are regenerated (keep small curated files, ignore bulky raw)
data/raw/*.jsonl
!data/raw/.gitkeep

# Model outputs / checkpoints (except the shipped adapter, which the CI golden
# gate loads from the repo)
outputs/*
!outputs/tutor-0.6b-v3
outputs/tutor-0.6b-v3/checkpoint-*
adapters/
merged/
*.gguf
*.safetensors
!outputs/tutor-0.6b-v3/*.safetensors
!outputs/tutor-0.6b-v3/**/*.safetensors

# Results are committed, but ignore scratch
results/scratch/

# Transient chat daemon I/O
demo/chat_io/

# OS
.DS_Store
Thumbs.db
